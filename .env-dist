# --- LLM Provider Selection ---
# Specify the LLM provider to use. Can be 'gemini' or 'openai'.
# If not specified, defaults to 'gemini'.
LLM_PROVIDER="gemini"

# --- Gemini Configuration ---
# Required if LLM_PROVIDER is 'gemini'.
# The system supports multiple API keys for rotation to avoid rate limits.
# It will use the primary GEMINI_API_KEY first, then look for GEMINI_API_KEY_0, GEMINI_API_KEY_1, and so on.
GEMINI_API_KEY="Your primary Gemini API Key"
# GEMINI_API_KEY_0="Your second Gemini API Key"
# GEMINI_API_KEY_1="Your third Gemini API Key"

# Optional: Defaults to 'gemini-2.5-pro' if not set.
# GEMINI_MODEL="gemini-2.5-flash"

# --- OpenAI Configuration ---
# Required if LLM_PROVIDER is 'openai'.
# OPENAI_API_KEY="Your OpenAI API Key Here"
# Optional: Defaults to 'gpt-4o' if not set.
# OPENAI_MODEL="gpt-4"

# --- Concurrency Setting ---
# Optional: Number of concurrent translation tasks. Defaults to 3.
# TRANSLATION_CONCURRENCY="2"

# --- Workspace Path ---
# The root path for the workspace. Defaults to './' if not set.
WORKSPACE_PATH="./"

# --- Language Setting ---
# Uncomment to use Traditional Chinese for UI messages.
# LANG="zh_TW.UTF-8"
